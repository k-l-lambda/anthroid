<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/overlay_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:padding="12dp"
    android:background="#E0424242"
    android:elevation="8dp">

    <!-- Robot head icon -->
    <ImageView
        android:id="@+id/overlay_icon"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:src="@drawable/ic_robot_active"
        android:layout_marginEnd="12dp" />

    <!-- Operation text with smooth scrolling -->
    <HorizontalScrollView
        android:id="@+id/text_scroll_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:scrollbars="none"
        android:overScrollMode="never">
        
        <TextView
            android:id="@+id/overlay_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Screen automation running..."
            android:textColor="#FFFFFF"
            android:textSize="18sp"
            android:singleLine="true"
            android:paddingEnd="48dp" />
            
    </HorizontalScrollView>

    <!-- Stop button (shown when active) -->
    <TextView
        android:id="@+id/overlay_stop_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="⏹"
        
        android:textSize="22sp"
        
        android:padding="8dp"
        android:background="?android:attr/selectableItemBackground"
        android:visibility="visible" />

    <!-- Close button (shown when inactive) -->
    <TextView
        android:id="@+id/overlay_close_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="✕"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:padding="8dp"
        android:background="?android:attr/selectableItemBackground"
        android:visibility="gone" />

</LinearLayout>
